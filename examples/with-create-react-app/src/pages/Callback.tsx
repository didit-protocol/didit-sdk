import { useEffect, useState } from 'react';

const Callback = () => (
  <div
    style={{
      display: 'flex',
      height: '100vh',
      justifyContent: 'center',
      alignItems: 'center',
    }}
  >
    <b>Loading...</b>
  </div>
);

export default Callback;
