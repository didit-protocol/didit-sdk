{
  "$schema": "https://codesandbox.io/schemas/tasks.json",
  "setupTasks": ["pnpm install didit-sdk didit-provider@latest", "pnpm build"],
  "tasks": {
    "install-dependencies": {
      "name": "Install Dependencies",
      "command": "pnpm install didit-provider@latest didit-sdk@latest"
    },
    "start-app": {
      "name": "Run Dev Server",
      "command": "pnpm dev",
      "runAtStart": true
    }
  }
}
